package ch06;

public class HouseKim {
    public static void main(String[] args) {
        HouseKim kim = new HouseKim();
        // default 접근제어자는 다른 패키지일 때 접근 불가
//         kim.lastname; // 접근 불가 (다른 패키지)
    }
}
